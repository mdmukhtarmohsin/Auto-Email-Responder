# AI-Powered Email Responder Configuration
# Copy this file to .env and fill in your values

# ==============================================================================
# REQUIRED CONFIGURATION
# ==============================================================================

# Gemini API Configuration (REQUIRED)
GOOGLE_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash-exp

# Gmail Configuration (REQUIRED for full functionality, optional for AI testing)
GMAIL_EMAIL_ADDRESS=your-email@gmail.com
GMAIL_CREDENTIALS_PATH=credentials.json
GMAIL_TOKEN_PATH=token.json

# ==============================================================================
# OPTIONAL CONFIGURATION
# ==============================================================================

# Vector Database Configuration
VECTOR_DB_TYPE=chroma
VECTOR_DB_PATH=./data/chroma_db
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Document Processing Configuration
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
TOP_K_DOCS=5
POLICIES_DIR=./config/policies

# Redis Configuration (Optional - for enhanced caching)
REDIS_URL=redis://localhost:6379/0
USE_REDIS_CACHE=false

# Application Configuration
LOG_LEVEL=INFO
BATCH_SIZE=10
PROCESSING_INTERVAL_MINUTES=5
MAX_RESPONSE_LENGTH=500

# Safety and Content Configuration
ENABLE_SAFETY_CHECKS=true
RESPONSE_TONE=polite

# Performance Configuration
CACHE_TTL_HOURS=24
MAX_EMAILS_PER_BATCH=50

# ==============================================================================
# USAGE NOTES
# ==============================================================================

# 1. Get Gemini API Key:
#    - Go to https://aistudio.google.com/
#    - Create an API key
#    - Set GOOGLE_API_KEY to your key

# 2. Setup Gmail API:
#    - Go to Google Cloud Console
#    - Enable Gmail API
#    - Create OAuth 2.0 credentials
#    - Download credentials.json file
#    - Set GMAIL_CREDENTIALS_PATH to the file path

# 3. Optional Redis Setup:
#    - Install Redis: sudo apt install redis-server
#    - Start Redis: sudo systemctl start redis
#    - Set USE_REDIS_CACHE=true for better performance

# 4. Email Configuration:
#    - Set GMAIL_EMAIL_ADDRESS to your Gmail address
#    - This is the email account that will send responses

# 5. Response Customization:
#    - RESPONSE_TONE: polite, friendly, professional, casual
#    - MAX_RESPONSE_LENGTH: Maximum characters in response
#    - PROCESSING_INTERVAL_MINUTES: How often to check for new emails

# 6. Performance Tuning:
#    - CHUNK_SIZE: Larger chunks for longer policies
#    - TOP_K_DOCS: More docs for better context (but slower)
#    - CACHE_TTL_HOURS: How long to cache responses 